<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <!-- Friendly: soft pink background with warm yellow accents -->
  <Color x:Key="BackgroundColor">#FFF6C0DB</Color>
  <!-- Make cards/surfaces a warm cream to let pink and yellow pop -->
  <Color x:Key="CardColor">#FFFFF6EE</Color>
  <Color x:Key="TextPrimaryColor">#FF2F2F2F</Color>
  <!-- Softer, warmer secondary text for better contrast on pink/cream -->
  <Color x:Key="TextSecondaryColor">#FF816565</Color>
  <!-- Slightly warmer yellow accent to pair with pink -->
  <Color x:Key="AccentColor">#FFF5C648</Color>

  <!-- Sidebar and surface keys so app areas update correctly -->
  <Color x:Key="SidebarColor">#FFFFF6EE</Color>
  <Color x:Key="SurfaceColor">#FFFFF6EE</Color>

  <!-- Selection (used for highlights) -->
  <Color x:Key="SelectionBackgroundColor">#FFE066A8</Color>
  <Color x:Key="SelectionForegroundColor">#FFFFFFFF</Color>

  <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
  <SolidColorBrush x:Key="CardBrush" Color="{StaticResource CardColor}"/>
  <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
  <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
  <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
  <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource AccentColor}"/>
  <SolidColorBrush x:Key="SidebarBrush" Color="{StaticResource SidebarColor}"/>
  <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}"/>
  <SolidColorBrush x:Key="SelectionBackgroundBrush" Color="{StaticResource SelectionBackgroundColor}"/>
  <SolidColorBrush x:Key="SelectionForegroundBrush" Color="{StaticResource SelectionForegroundColor}"/>
</ResourceDictionary>
